{
  "name": "samshir-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve . -l 8081",
    "build": "echo 'No build step required'",
    "preview": "npx serve . -l 8081",
    "start": "npx serve . -l 8081"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
